apiVersion: apps/v1
kind: Deployment
metadata:
  name: productos-get-deployment
  labels:
    app: prod-get-pablo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: prod-get-pablo
  template:
    metadata:
      labels:
        app: prod-get-pablo
    spec:
      containers:
      - name: prod-get-pablo
        image: palandeta/prod-get-pablo-abr26
        ports:
        - containerPort: 5000
        env:
        - name: MONGO_URI
          value: "mongodb://mongodb-service:27017"
        - name: MONGO_DB
          value: "productosdb"
        - name: MONGO_COLLECTION
          value: "products"
        resources:
          limits: 
            cpu: "300m"
            memory: "256Mi"
        readinessProbe:
          httpGet:
            path: /health
            port: 5000
          initialDelaySeconds: 10
          periodSeconds: 5
        livenessProbe:
          httpGet:
            path: /health
            port: 5000
          initialDelaySeconds: 15
          periodSeconds: 20            